# Machine-local tooling, not project source.
.claude/
.agents/
.playwright/
skills-lock.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Downloaded separately via backend/scripts/download_weights.py — the
# checkpoint alone is 392MB, well past GitHub's 100MB file limit.
backend/weights/

# Real secrets — only .env.example is meant to be committed.
backend/.env
.env

# OS cruft
.DS_Store
Thumbs.db
